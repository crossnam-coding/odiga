> ⚠️ **이 문서는 2026-08-03 저녁 상태다. 내용이 틀린 곳이 있다.**
> 최신은 `HANDOFF_20260808_어디가지.md` — 그것부터 읽을 것.

# HANDOFF — 어디가지(odiga) 조건 검색 서비스 (2026-08-03)

## 0. 첫 read 파일 순서

1. 이 파일
2. `/Users/ros/.claude/실패판정대장.md` 7번 섹션 — 이 분야 사업 판정(국내외 전멸 부검)
3. `/Users/ros/존버맵/functions/api/search.js` — 백엔드 전부. 주석에 실측 근거가 함께 있다
4. `/Users/ros/존버맵/프로필.md` — 오빠 개인 조건 8개 (git 제외 파일. 공개 금지)
5. `/Users/ros/존버맵/README.md`

---

## 1. 단일 목표

**오빠가 실제로 계속 쓰게 만든다.** 판정 기준은 매출이 아니라 **오빠가 30일 안에 몇 번 여는가**.

오빠 확정 발언: *"우선 내가 불편해서 필요해 계속 해봐"* · *"나부터 쓰게 만들어봐"*

이 밖은 스코프 이탈이다. 특히 **수익화·유료화 설계는 지금 하지 않는다** (아래 §4 참조).

---

## 2. 실측 현황

### 라이브 (2026-08-03 21:00 실측)

| 항목 | 값 | 확인 방법 |
|---|---|---|
| 서비스 URL | **https://odiga-eyf.pages.dev** | HTTP 200 |
| `/api/search` | **200, 후보 6곳, 1.7초** | 브라우저에서 실제 클릭 |
| GitHub repo | https://github.com/crossnam-coding/odiga (public) | 미커밋 0건, 푸시 완료 |
| GitHub Pages | **비활성화함** | `gh api .../pages` → 404 |

⚠️ **URL은 하나뿐이다.** `crossnam-coding.github.io/odiga` 는 API가 없는 구버전이라 이 세션에서 껐다.
화면상 잠시 200으로 보이면 CDN 잔상이다.

### 네이버 검색 API (NAVER API HUB / NCP)

| | |
|---|---|
| Application | `odiga` · 계정 (주)엔씨이 · 2026-08-03 19:41 등록 |
| 사용 API | 블로그 · 지역 · 이미지 · 웹문서 (4개, 전부 HTTP 200 검증) |
| 한도 | **당일 25,000 / 당월 775,000 · 요금 0원** |
| 사용량 | 등록 시점 0/25,000 |

**엔드포인트 (문서에 잘 안 드러나 경로 후보 12개를 때려서 찾음):**
```
https://naverapihub.apigw.ntruss.com/search/v1/blog
헤더: X-NCP-APIGW-API-KEY-ID / X-NCP-APIGW-API-KEY
```
함정 4개 — ①호스트가 `naveropenapi`가 아니라 **`naverapihub`** ②경로가 `/v1/search/blog`가 아니라
**`/search/v1/blog`**(순서 반대) ③**`.json` 붙이면 404** ④헤더가 `X-Naver-Client-Id`가 **아니다**.

### 키 보관

- `/Users/ros/존버맵/.env` (권한 600, gitignore됨) — 네이버 키 2개 + Cloudflare 토큰
- Cloudflare Pages 시크릿에도 주입됨 (`NCP_APIGW_KEY_ID`, `NCP_APIGW_KEY`)
- **git 히스토리·배포본 어디에도 키 노출 0건** (문자열 검색으로 확인)
- Cloudflare 토큰 권한: **Cloudflare Pages:편집 하나뿐** (DNS·Workers 권한 없음)

### 동작 원리 (핵심 설계)

**협찬 여부를 판별하지 않는다.** 실측 결과 협찬 표기 검출률이 본문 **14.2%(17/120)**, 검색 요약
**1.1%(1/90)**에 그쳐 판별 자체가 성립하지 않았다. 게다가 **랭킹 상위일수록 표기가 없다**
(다몽집 상위30 = 0%).

대신 **협찬 글이 쓰지 않는 정보만 뽑는다** — 콘센트 개수, 에어컨, 체류 시간, 가격, 주차.
30분 있다 사진 찍고 나간 사람은 이런 걸 못 쓴다.

정렬은 **언급 횟수가 아니라 조건 입증 개수**다. 실측 근거: 골든트리는 검색 12회 노출인데
본문 40,262자에 콘센트·에어컨·가격 언급이 **0건**이었다.

---

## 3. 다음 할 일

1. **오빠가 실제로 쓴 결과를 받는다.** 무엇을 검색했고 결과가 맞았는지. 이게 판정 데이터다.
2. **미결 질문**: 오빠가 8/3 저녁에 송원/정원가든 중 어디를 갔는지 아직 답을 못 받았다.
   첫 실측 검증이므로 물어볼 것.
3. **지인 공유 + 데이터 수집** — 오빠 요청사항이나 아직 미착수.
   *"지인들하고 같이 써서 데이터 받아야하니"*. 검색 로그를 어디에 쌓을지부터 정해야 한다.
4. **프로필 자동 축적** — 지금은 `프로필.md`를 사람이 손으로 갱신한다. 검색할 때마다
   조건이 프로필에 쌓이게 만들면 개인화가 자동으로 굴러간다.
5. **대장 박제** — 이 세션에서 알아낸 함정들(네이버 API 경로·wrangler·캐시)이 아직
   `~/.claude/실패판정대장.md`에 안 들어갔다. §4 아래 목록 그대로 옮기면 된다.

---

## 4. 🚫 재시도 금지

### 사업 방향

- **"가짜/협찬 리뷰를 판별해주는" 소비자 서비스** — 국내외 전멸 확정.
  국내 `1Sec`(크롬확장, 이미지 pHash까지 구현) **사용자 4명**. 해외 Fakespot·ReviewMeta·
  TheReviewIndex **3개 전부 7개월 안에 사망**. Mozilla 공식 사인: *"it didn't fit a model we could sustain"*.
  → 대장 7번 섹션에 근거 전부 있음. **다시 조사하지 마라.**
- **소비자에게 신뢰를 파는 수익모델** — 지불의사 0에 수렴. 이 바닥 돈은 전부
  "리뷰를 관리하고 싶은 쪽"(Trustpilot $261M, TraceFuse 건당 $250)에 있다.

### 기술

- **네이버 검색 API를 서비스 기반으로 쓰는 것(약관)** — 유료화 금지(7.2③)·광고 금지(7.3④)·
  캐시 저장 금지(7.3③). **개인 도구인 동안은 무방하나 제품화 시 데이터원을 바꿔야 한다.**
- **`*.place.naver.com`(영수증 리뷰)** — curl 429 / WebFetch·브라우저 policy 차단. 4경로 전부 막힘.
- **`blog.naver.com/<id>/<logNo>` 로 본문 읽기** — HTTP 200인데 **가시 텍스트 20자**(프레임셋 껍데기).
  `PostView.naver` + **데스크탑 UA** 를 써야 전문이 온다. 모바일 UA면 JS 리다이렉트 스텁만 온다.
- **네이버 카페 검색** — "가평 카페 오래 앉아있기"로 검색하니 허경영 강연·원균 논쟁이 나왔다.
  이 용도로 못 쓴다. Application에서도 뺐다.
- **조건 문장을 그대로 지역 검색에 넣기** — 결과 0. 종류 명사만 뽑아야 한다.
- **wrangler 로 프로젝트 루트 배포** — `.gitignore` 를 보지 않아 **42개 파일이 통째로 올라갔고
  `프로필.md`(오빠 개인정보)가 섞였다.** 반드시 `./deploy.sh` 로만 배포할 것.
- **배포 후 브라우저로만 확인하고 판단하기** — 캐시된 구버전을 보고 "안 고쳐졌다"고 두 번 오판했다.
  `curl` + 해시 대조로 확인할 것.

---

## 5. 롤백 방법

```bash
# 배포 되돌리기 — 이전 배포 URL 로 승격
npx wrangler@4 pages deployment list --project-name odiga
# 또는 코드 되돌리고 재배포
cd /Users/ros/존버맵 && git revert <sha> && ./deploy.sh

# 네이버 키 무효화 (노출 의심 시)
# NCP 콘솔 > NAVER API HUB > Application > odiga > 인증 정보 > [재발급]

# Cloudflare 토큰 무효화
# dash.cloudflare.com > 내 프로필 > API 토큰 > odiga-pages-deploy > 삭제
```

**배포는 반드시 `./deploy.sh`** — 비밀 파일이 섞이면 스크립트가 중단시킨다.

---

## 부록 — 이 세션에서 오빠가 확정한 것

| 오빠 발언 | 확정 내용 |
|---|---|
| *"우선 내가 불편해서 필요해"* | 판정 기준을 유료 3명 → **오빠 사용 횟수**로 변경 |
| *"이 느낌을 원해"* (가평 카페 예시) | 제품 정의 = **복합 조건 질의 + 근거 제시**. 맛집 추천 앱이 아니다 |
| *"나 중심에서 알려주면 되잖아"* | 내 위치 · 이동수단 · 시간 반경이 필수 축 |
| *"지인들하고 같이 써서 데이터 받아야"* | 다중 사용자 + 로그 수집이 다음 단계 |
| *"버셀도 좀 한계가 있고"* | Vercel 금지 — 대장 153번 팀 계정 정지 이력 (혼빛 동반 정지 위험) |

**미결 (오빠 답 대기): 8/3 저녁에 어디 갔는지.**
