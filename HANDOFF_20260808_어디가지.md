# HANDOFF — 어디가지(odiga) (2026-08-08)

> 8/3 세션에서 하루 만에 기획→배포까지 끝냈고, 8/3 밤에 오빠 실사용 피드백으로 크게 고쳤다.
> 이 문서가 최신이다. `HANDOFF_20260803_어디가지.md`는 **하루 전 상태라 내용이 틀리다** (§5 참조).

## 0. 첫 read 파일 순서

1. 이 파일
2. `/Users/ros/.claude/실패판정대장.md` 7번 섹션 — 이 분야 사업 판정(국내외 전멸 부검)
3. `/Users/ros/존버맵/functions/api/search.js` — 백엔드 전부. 주석에 실측 근거가 함께 있다
4. `/Users/ros/존버맵/assets/regions.js` — 지역 인식. 왜 추측하면 안 되는지 주석에 있다
5. `/Users/ros/존버맵/프로필.md` — 오빠 개인 조건 8개 (**git 제외 · 공개 금지**)

---

## 1. 단일 목표

**오빠가 실제로 계속 쓰게 만든다.** 판정 기준은 매출이 아니라 **오빠가 몇 번 여는가**.

오빠 확정 발언: *"우선 내가 불편해서 필요해 계속 해봐"* · *"나부터 쓰게 만들어봐"*

**수익화·유료화 설계는 지금 하지 않는다** (§4 참조).

---

## 2. 실측 현황 (2026-08-08 18:43 확인)

| 항목 | 값 |
|---|---|
| 서비스 | **https://odiga-eyf.pages.dev** · HTTP 200 |
| GitHub | https://github.com/crossnam-coding/odiga · **미커밋 0건** |
| GitHub Pages | **꺼짐** (404) — URL 혼동 방지로 8/3에 비활성화 |
| 마지막 배포 | 2026-08-03 22:53 (`app.js?v=202608032253`) |
| 로컬 vs 라이브 | **해시 동일** — 미배포 변경 없음 |
| 네이버 키 | 살아있음 (HTTP 200) |

### 네이버 검색 API (NAVER API HUB / NCP)

Application `odiga` · 계정 (주)엔씨이 · **당일 25,000 / 당월 775,000 · 요금 0원**
사용 API: 블로그 · 지역 · 이미지 · 웹문서

```
https://naverapihub.apigw.ntruss.com/search/v1/blog
헤더: X-NCP-APIGW-API-KEY-ID / X-NCP-APIGW-API-KEY
```
함정 4개 — ①호스트가 `naveropenapi`가 아니라 **`naverapihub`** ②경로가 `/v1/search/blog`가 아니라
**`/search/v1/blog`**(순서 반대) ③**`.json` 붙이면 404** ④헤더가 `X-Naver-Client-Id`가 **아니다**.
경로 후보 12개를 시도해 찾았다.

### 키 보관

- `.env` (권한 600, gitignore) — 네이버 키 2개 + Cloudflare 토큰
- Cloudflare Pages 시크릿에도 주입됨
- **git 히스토리·배포본에 키 노출 0건** (문자열 검색 확인)
- Cloudflare 토큰 권한: **Cloudflare Pages:편집 하나뿐**

### 동작 (핵심 설계 3가지)

**① 협찬 여부를 판별하지 않는다.** 표기 검출률이 본문 14.2%(17/120)·요약 1.1%(1/90)에
그쳤고, 랭킹 상위일수록 표기가 없었다(다몽집 상위30 = 0%). 대신 **협찬 글이 쓰지 못하는
정보**만 본다 — 콘센트·에어컨·체류·가격·주차.

**② 네이버가 준 순서를 쓰지 않는다.** 그건 노출 순위다. `재방문·비교우위·현지·단점인정`
같은 문장을 근거로 세고 `조건근거 + 좋은근거×2 − 경고` 로 다시 정렬한다.
실측: 골든트리는 검색 12회 노출인데 본문 40,262자에 콘센트·에어컨·가격 언급이 0건이었다.

**③ 모르는 건 채우지 않는다.** 근거를 못 찾으면 "여기가 좋다는 근거는 못 찾았어 —
조건만 맞는 곳이야"라고 표시한다.

---

## 3. 다음 할 일

1. **🔴 오빠가 8/3 이후 실제로 썼는지 확인한다.** 이게 판정 데이터다.
   5일이 지났고 배포 이력은 8/3이 마지막이다. 안 썼다면 왜 안 썼는지가 핵심 정보다.
   (사용량은 NCP 콘솔 > NAVER API HUB > Usage Statistics 에서 볼 수 있다. 토큰 권한으로는 못 본다.)
2. **지인 공유 + 데이터 수집** — 오빠 요청사항이나 미착수.
   *"지인들하고 같이 써서 데이터 받아야하니"*. 검색 로그를 어디에 쌓을지부터.
3. **프로필 자동 축적** — 지금은 `프로필.md`를 사람이 손으로 갱신한다.
4. **대장 박제** — 이 세션 함정들이 아직 `~/.claude/실패판정대장.md`에 없다. §4 목록 그대로.

---

## 4. 🚫 재시도 금지

### 사업 방향
- **"가짜/협찬 리뷰 판별" 소비자 서비스** — 국내외 전멸 확정. 국내 `1Sec` 사용자 4명,
  해외 Fakespot·ReviewMeta·TheReviewIndex 3개 전부 7개월 안에 사망.
  Mozilla 공식 사인: *"it didn't fit a model we could sustain"*. → 대장 7번 섹션. **재조사 금지.**
- **소비자에게 신뢰를 파는 수익모델** — 지불의사 0. 돈은 "리뷰를 관리하고 싶은 쪽"에 있다.

### 기술
- **네이버 검색 API를 서비스 기반으로** — 약관이 유료화(7.2③)·광고(7.3④)·캐시저장(7.3③)을 금지.
  개인 도구인 동안은 무방하나 **제품화 시 데이터원을 바꿔야 한다.**
- **`*.place.naver.com`(영수증 리뷰)** — 4경로 전부 차단.
- **`blog.naver.com/<id>/<logNo>`로 본문 읽기** — 200인데 가시 텍스트 20자(프레임셋).
  `PostView.naver` + **데스크탑 UA** 필수. 모바일 UA면 JS 리다이렉트 스텁만 온다.
- **네이버 카페 검색** — "가평 카페 오래 앉아있기"에 허경영 강연·원균 논쟁이 나왔다. 못 쓴다.
- **조건 문장을 그대로 지역 검색에 넣기** — 결과 0.
- **지역명을 문장에서 추측하기** — 접미사로 찾으면 "가능하**면**"이 걸리고, 첫 어절을 쓰면
  "조미료"·"에어컨"이 지역이 된다. **`assets/regions.js` 사전으로만** 인정할 것.
- **`wrangler`로 프로젝트 루트 배포** — `.gitignore`를 보지 않아 42개가 올라갔고
  **`프로필.md`(개인정보)가 섞였다.** 반드시 `./deploy.sh`로만 배포.
- **`_headers`에 `/index.html`만 쓰기** — 루트(`/`)에 규칙이 안 먹어 배포해도 옛 화면이 보인다.
- **배포 후 브라우저로만 확인** — 캐시된 구버전을 보고 "안 고쳐졌다"고 세 번 오판했다.
  `curl` + 해시 대조로 확인할 것.
- **API 응답에 긴 캐시** — max-age=300이면 배포해도 5분간 옛 결과가 나온다. 현재 60초.

---

## 5. 8/3 HANDOFF와 달라진 것 (모순 대조 결과)

`HANDOFF_20260803_어디가지.md`는 **8/3 저녁 상태**라 아래가 틀리다. 그 문서를 먼저 읽지 말 것.

| 항목 | 8/3 문서 | 실제 (8/8) |
|---|---|---|
| 지역 인식 | 언급 없음 | **지명 사전** 필수 (`regions.js`) |
| 좌표→지역 | 없음 | **`/api/where`** 로 자동 |
| "좋은 집" 근거 | 없음 | **`good` 배열 + 광고 말투 표시** |
| 정렬 | 조건 근거 개수 | **조건 + 좋은근거×2 − 경고** |
| 초기 화면 | 가평 6곳 표시 | **아무것도 안 보여줌** (하드코딩 제거) |
| 미결 질문 | "8/3 저녁 어디 갔는지" | **무의미해짐** — 5일 지남. 대신 "썼는지"를 물을 것 |

---

## 6. 롤백

```bash
cd /Users/ros/존버맵
npx wrangler@4 pages deployment list --project-name odiga   # 이전 배포 확인
git revert <sha> && ./deploy.sh                              # 코드 되돌리고 재배포

# 네이버 키 무효화: NCP 콘솔 > NAVER API HUB > Application > odiga > 인증 정보 > [재발급]
# CF 토큰 무효화: dash.cloudflare.com > 내 프로필 > API 토큰 > odiga-pages-deploy > 삭제
```
**배포는 반드시 `./deploy.sh`** — 비밀 파일이나 키 문자열이 섞이면 스크립트가 중단시킨다.

---

## 부록 — 오빠가 확정한 것 / 오빠가 잡아낸 버그

| 오빠 발언 | 결과 |
|---|---|
| *"우선 내가 불편해서 필요해"* | 판정 기준 = **오빠 사용 횟수** (유료 3명에서 변경) |
| *"이 느낌을 원해"* (가평 카페 예시) | 제품 정의 = **복합 조건 질의 + 근거 제시**. 맛집 추천 앱이 아니다 |
| *"나 중심에서 알려주면 되잖아"* | 위치·이동수단·시간 반경이 필수 축 |
| *"지인들하고 같이 써서 데이터 받아야"* | 다중 사용자 + 로그 수집이 다음 단계 |
| *"버셀도 좀 한계가 있고"* | Vercel 금지 — 대장 153번 팀 계정 정지 이력 |
| ***"이게 맛집이라는 근거가 뭐죠??"*** | **제품의 방향을 바로잡은 질문.** 네이버 순서를 그대로 쓰던 걸 근거 기반으로 전환 |
| *"단점 인정인데 글은 좋은글이야"* | "웨이팅은 기본!"이 단점인정으로 오탐되던 것 수정 |
| *"봇이나 알바 댓글 같은 것도 알려주면"* | 광고 말투·대가성 밝힘 배지 추가 |
| *"생선 먹고 싶다는데 베이커리 나옴"* | 음식 사전 확장 + 사전에 없는 음식 폴백 |
| *"여전히 가평이야 / 난 지금 용인인데"* | 가평 하드코딩 3곳 제거 |
