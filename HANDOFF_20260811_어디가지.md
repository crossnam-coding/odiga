# HANDOFF — 어디가지(odiga) 디자인 개편 (2026-08-11)

> 8/8 HANDOFF 이후 8/9·8/11 두 날에 걸쳐 **홈 전체를 "조수석 앞 테이블" 씬으로 다시 만들었다.**
> `HANDOFF_20260808_어디가지.md` 는 **그 이전 상태**라 §2 실측·§4 금지 목록만 유효하고
> 화면 구성 설명은 전부 낡았다. 이 문서가 최신이다.

## 0. 첫 read 파일 순서

1. 이 파일
2. `/Users/ros/.claude/실패판정대장.md` — 7번 섹션(사업 판정) + 178~190행(이 프로젝트 함정 12개)
3. `/Users/ros/존버맵/assets/app.js` — 프론트 전부. 각 기능 위 주석에 "왜 이렇게 됐는지"가 있다
4. `/Users/ros/존버맵/functions/api/search.js` — 백엔드 전부
5. `/Users/ros/존버맵/프로필.md` — 오빠 개인 조건 (**git 제외 · 공개 금지**)
6. `/Users/ros/.claude/skills/멋진사이트/SKILL.md` — 이 개편에 쓴 디자인 판정 절차

---

## 1. 단일 목표

**오빠가 실제로 계속 쓰게 만든다.** 판정 기준은 매출이 아니라 **오빠가 몇 번 여는가**.

8/8에 확인된 사실: 배포 후 5일간 **사용 0회**, 이유는 "아예 안 열었다" = **진입점 문제**였다.
그 뒤 홈화면 설치·사용 카운터·저장 기능을 넣었고, 지금은 **디자인 완성도**를 올리는 중이다.

수익화·유료화 설계는 지금 하지 않는다.

---

## 2. 실측 현황 (2026-08-11 12:5x 확인)

| 항목 | 값 |
|---|---|
| 서비스 | https://odiga-eyf.pages.dev · **HTTP 200** |
| **라이브 배포본** | `app.js?v=202608092130` = 커밋 **`9c097a0`** (8/9 21:30 배포) |
| 로컬 main | **`b6c468d`** · 미커밋 **0건** |
| **라이브 대비 미배포** | **6커밋** (아래 목록) |
| 🔴 **GitHub 대비 미푸시** | **12커밋** — `origin/main` 이 아직 `7026cde`(8/8) |
| 로컬 서버 | http://127.0.0.1:8788 · HTTP 200 (세션 바뀌면 죽는다 → §5) |

### 🔴 GitHub 백업이 안 돼 있다

`origin/main` = `7026cde`(8/8 인수인계 문서). **8/9·8/11 작업 12커밋이 전부 로컬에만 있다.**
글로벌 규칙(💾 검증된 백업)상 이건 사고 대기 상태다. **다음 세션 첫 일로 push 를 권한다.**

```bash
cd /Users/ros/존버맵 && git push origin main --tags
```

### 라이브 대비 미배포 6커밋

```
b6c468d 밝게/어둡게 토글을 단다
7e6faea 화면 전체를 한 세계로 — 손글씨 로고와 종이 톤 통일
0afb1f2 씬을 채운다 — 종이는 종이색으로, 선은 굵고 진하게
818bdc5 사물마다 자기 움직임을 준다, 지역은 쳐서 찾는다
8915e0e 사물에서 글자 라벨을 떼고, 안 눌러본 것이 반짝이게 한다
9476afd 사물 넷에 각자 할 일을 준다 — 포스트잇·컵·지도·서랍
```

**오빠가 아직 이 6개를 눈으로 못 봤다** (로컬에서만 확인). 배포 승인 대기 중.

### 지금 화면이 어떻게 생겼나

**홈 = 조수석 앞 테이블 씬 한 장** (SVG `viewBox 400×690`, index.html 인라인).
컨트롤이 씬 위에 놓인 사물이다. 씬을 통과해 도구로 가는 게 아니라 **씬이 곧 도구**다.

| 사물 | 기능 | 상태 표시 |
|---|---|---|
| 앞유리 | 지금 어디 있는지 (+시간대별 해·달·별) | 위치 문구 |
| 스프링 노트 | **조건 입력칸** | — |
| 메모지 뭉치 | **가보고 싶은 곳** 저장 | 개수 배지 |
| 커피잔 | **다녀온 곳** | 개수 배지 |
| 글로브박스 | **늘 챙길 조건**(프로필) | 개수 배지 |
| 접힌 지도 | **어디서 찾을지**(지역 선택) | 지역 이름 배지 |
| 차키 / 발자국 | 이동수단 | 고른 쪽만 이름 |
| 돋보기 | 찾아줘 | — |

- **사물 밑에 글자 라벨을 달지 않는다.** 발견성은 "안 눌러본 사물이 반짝인다"로 푼다.
- 사물마다 자기 움직임: 메모지 펄럭 · 커피 김 · 서랍 달그락 · 지도 음파.
- 저장은 전부 **브라우저 localStorage**. 서버로 안 보낸다.
  키: `odiga.use.v1` `odiga.profile.v1` `odiga.saved.v1` `odiga.region.v1`
  `odiga.recentRegion.v1` `odiga.seen.v1` `odiga.theme.v1` `odiga.install.snooze`

### 백엔드 (8/9에 고친 것)

오빠가 `"엄마랑 저녁 75세 한식 좋아하심"` 으로 검색했더니 **키즈카페·여성의류·인터넷상거래**가 나왔다.
원인 4개를 전부 고쳤고 라이브에 이미 반영돼 있다:

1. `KINDS` 에 **'한식'이 없었다** → 폴백으로 새어 "엄마랑"·"저녁"이 검색어가 됐다
2. 어르신 조건이 **"어머니"만** 봤다 → 엄마·아빠·`\d+세` 추가
3. 사람·시간·서술어가 `NOT_KIND` 에 없어 검색어에 남았다
4. **업종 필터가 없었다** → `FOOD` 정규식 신설 (앞자리 고정 `^` 은 "브런치,카페"를 떨어뜨리니 **포함 매칭**)

검증: `asked=['어르신']`, 5곳 전부 음식점. 질의 5종 회귀 통과.

---

## 3. 다음 할 일

1. **🔴 `git push origin main --tags`** — 12커밋이 로컬에만 있다. 제일 먼저.
2. **배포 승인 받고 `./deploy.sh`** — 미배포 6커밋. 오빠 "배포해/ㄱㄱ" 후에만.
   배포 후 **전파가 늦다**(§4) — curl 로 10회 뽑아 전부 신버전인지 확인할 것.
3. **오빠 눈 확인 대기 2건** (로컬에서만 봤고 오빠 판단 필요)
   - 사물 애니메이션 **속도·진폭**이 적당한지 (서랍 달그락은 6초에 한 번이라 기다려야 보인다)
   - 다크 / 라이트 중 어느 쪽으로 갈지 (**라이트가 훨씬 근사하다**는 게 클로드 의견)
4. **오빠가 던져둔 다음 후보** (아직 답 안 함)
   - 결과 카드를 살짝 기울여 흩어놓은 종이처럼 (읽기엔 불리)
   - 근거 인용에 형광펜 마킹
   - 찾는 중일 때 씬 전체 반응 (와이퍼가 한 번 지나간다든가)
5. **홈화면 설치 → 사용 카운터 확인.** 이게 이 프로젝트의 판정 데이터다.
   화면 맨 아래 "이번 달 N번 열고 M번 찾았어 · 홈화면에서 K번".
   **8/8 이후 오빠가 폰에 깔았는지 아직 확인 못 했다.**
6. 지인 공유 + 서버 로그 수집 — 8/8부터 미착수. 지금은 전부 브라우저 저장이라 오빠 것만 쌓인다.

---

## 4. 🚫 재시도 금지

### 사업 방향 (대장 7번 섹션 · 영구)
- **"가짜/협찬 리뷰 판별" 소비자 서비스** — 국내외 전멸 확정. 재조사 금지.
- **소비자에게 신뢰를 파는 수익모델** — 지불의사 0.

### 기술 (대장 178~190행에 박제됨)
- **네이버 검색 API를 제품 기반으로** — 약관이 유료화·광고·캐시저장 금지. 개인 도구인 동안만 무방.
- `*.place.naver.com`(영수증 리뷰) · 네이버 **카페** 검색 · 조건 문장을 그대로 지역 검색에.
- **지역명을 문장에서 추측** — `assets/regions.js` 사전으로만.
- **`wrangler` 로 프로젝트 루트 배포** — `.gitignore` 를 안 본다. 반드시 `./deploy.sh`.
- **배포 후 브라우저로만 확인** — `curl` + 해시 대조로. 브라우저는 캐시된 구버전을 보여준다.

### 이번 개편에서 새로 확정된 것
- **사물 밑에 글자 라벨 달기** — 배치가 지저분해진다(오빠 지적). 반짝임으로 대체했다.
- **20px 아이콘에 신발 실루엣** — 측면은 옷걸이, 위에서 본 것은 리모컨으로 읽혔다. **발자국으로 확정.**
- **버튼에 `textContent` 대입** — 안의 아이콘 `<svg>` 가 지워진다. 라벨은 `<span>` 으로 분리할 것.
- **`.note` 클래스를 새 요소에 재사용** — "어떻게 고르는지" 안내 박스가 이미 쓰는 이름이라 규칙이 덮인다.
- **가로형(4:3) 씬에 노트 넣기** — 375px 에서 입력칸이 105px 이 된다. 세로형이라야 285px.
- **김·음파를 손그림 필터 안에 두기** — 매 프레임 필터 재계산 위험. 필터 밖에 뒀다.

### 검증할 때 조심 (이번에 세 번 헛짚었다)
- **`localhost` 와 `127.0.0.1` 은 localStorage 가 별개다.** 한쪽에서 지우고 다른 쪽에서 확인하면 안 지워진 것처럼 보인다.
- **브라우저 pane 스크린샷은 한 프레임 늦다.** 테마·상태를 바꾼 직후 찍으면 이전 화면이 나온다. 값은 `getComputedStyle` 로 확인.
- **pane 이 백그라운드면 CSS 애니메이션이 0에서 멈춘다**(`visibility:hidden`). 움직임 검증은 `getAnimations()` 로 `currentTime` 을 옮겨서.
- **`/assets/*` 는 1년 immutable.** `?v=` 없이 curl 하면 옛 캐시가 온다. 반드시 라이브가 부르는 버전 스탬프를 붙여 대조.

---

## 5. 롤백 방법

```bash
cd /Users/ros/존버맵

# 라이브 되돌리기 (지금 라이브 = 9c097a0 = rollback-20260809-a74b9db 다음 커밋)
git reset --hard <태그> && ./deploy.sh

# 쓸 수 있는 태그
#   rollback-20260809-a74b9db   글로브박스 발견성 수정 직전
#   rollback-20260809-425f710   글로브박스 도입 직전
#   rollback-20260809-fd596e7   씬 전면개편 직전 (예전 폼 화면)

# 이전 배포 목록
npx wrangler@4 pages deployment list --project-name odiga
```

**배포는 반드시 `./deploy.sh`.** 비밀 파일·키 문자열이 섞이면 스크립트가 중단시킨다.
`./deploy.sh --dry` 는 올리지 않고 번들만 확인한다(승인 전 검증용).

### 로컬 서버 (세션 바뀌면 죽는다)

`.claude/launch.json` 의 `odiga` 항목으로 preview_start. 직접 띄우면:

```bash
cd /Users/ros/존버맵 && ./deploy.sh --dry >/dev/null && \
  npx --yes wrangler@4 pages dev dist --port 8788 --ip 127.0.0.1 --compatibility-date 2026-05-03
```

⚠️ `--compatibility-date` 를 안 낮추면 **"Workers runtime failed to start"** 로 안 뜬다
(wrangler.toml 의 `2026-08-03` 이 로컬 런타임 상한보다 미래다). **wrangler.toml 은 건드리지 말 것** —
프로덕션 동작이 바뀐다.

---

## 부록 — 미해결로 남겨둔 것

| 항목 | 상태 |
|---|---|
| **`wrangler.toml` 의 `pages_build_output_dir = "."`** | 아직 루트를 가리킨다. 누가 `wrangler pages deploy` 를 그냥 치면 8/3 프로필.md 유출 사고가 재현된다. 라이브에 `wrangler.toml` 이 실제 서빙 중이나 **비밀 문자열 0건**(실측). deploy.sh 인자와 같이 고쳐야 해서 손대지 않았다 |
| 프로필.md 방문 이력 | 이제 앱의 "컵"에 쌓인다. **md 파일 자동 갱신은 안 한다**(브라우저 저장이라 파일로 못 내린다) |
| 서버측 사용 집계 | 없음(코드 실측). 8/8 기록엔 "CF 토큰 = Pages:편집뿐이라 KV/D1 생성 불가"로 돼 있으나 **이번에 재확인 실패** — `wrangler whoami` 로는 권한 조회가 안 되고 대시보드(`dash.cloudflare.com/profile/api-tokens`)에서만 보인다. **KV 가 정말 불가한지는 확인 필요**로 남긴다 |
